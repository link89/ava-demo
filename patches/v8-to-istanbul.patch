diff --git a/node_modules/v8-to-istanbul/lib/v8-to-istanbul.js b/node_modules/v8-to-istanbul/lib/v8-to-istanbul.js
index d1cd6f4..d2f8705 100644
--- a/node_modules/v8-to-istanbul/lib/v8-to-istanbul.js
+++ b/node_modules/v8-to-istanbul/lib/v8-to-istanbul.js
@@ -43,6 +43,11 @@ module.exports = class V8ToIstanbul {
       convertSourceMap.fromSource(rawSource) || convertSourceMap.fromMapFileSource(rawSource, dirname(this.path))
 
     if (rawSourceMap) {
+      this.source = new CovSource(rawSource, this.wrapperLength)
+    }
+    return
+
+    if (rawSourceMap) {
       if (rawSourceMap.sourcemap.sources.length > 1) {
         console.warn('v8-to-istanbul: source-mappings from one to many files not yet supported')
         this.source = new CovSource(rawSource, this.wrapperLength)
